export const metadata = {
  title: "4 Letter Domain Names Checker | Bulk Domain Search Tool",
  description: "Search available 4 letter domain names with our bulk domain checker. Find unregistered CVCV pattern domains (like BAKA, KUMA) instantly. Free domain availability search tool.",
};

export default function FourLetterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 